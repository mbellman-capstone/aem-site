{
  "name": "aem-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node serve.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf aem-build && node build.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.21.0",
    "open": "^10.1.0"
  }
}
